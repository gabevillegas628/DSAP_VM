{
  "name": "dna-analysis-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npx prisma generate && node index.js",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm start\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}